package xyz.dwaslashe.abyss.utils;

public class BossBarApi {

    //public static void sendGlobal(BarColor color, BarStyle style, int time, String message) {
    //    org.bukkit.boss.BossBar bar = Bukkit.createBossBar(Api.fixColor(message), color, style, new BarFlag[0]);
    //    if (Main.pluginConfig.getConfigureMessages().getBossBar().isEnable()) {
    //        Bukkit.getOnlinePlayers().forEach(player -> {
    //            bar.addPlayer(player);
    //        });
    //    }
    //    bar.setProgress(1);
    //    Main.getMain().getServer().getScheduler().runTaskLater(Main.getMain(), bar::removeAll, time * 20);
    //}
}
